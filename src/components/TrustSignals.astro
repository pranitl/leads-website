---
const {
  stats = [
    { label: 'Qualified specialists', value: '150+', detail: 'Landscape designers, irrigation techs, and interior fabricators.' },
    { label: 'Avg. match time', value: '<24h', detail: 'Concierge outreach within one business day.' },
    { label: 'Satisfaction score', value: '4.9/5', detail: 'Post-project homeowner feedback across markets.' }
  ],
} = Astro.props;
---

<section class="rounded-3xl border-2 border-brown-forest/20 bg-cream-light p-6 shadow-hard-sm lg:p-10">
  <div class="flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between">
    <div class="max-w-xl space-y-3">
      <p class="text-xs font-semibold uppercase tracking-[0.25em] text-brown-forest">Why homeowners choose us</p>
      <h2 class="font-heading text-3xl font-semibold text-brown-dark">Premium artisans, vetted process, concierge follow-through</h2>
      <p class="text-sm text-brown-body">Every specialist in our network is reference-checked, insured, and aligned with sustainable building practices.</p>
    </div>
    <div class="grid w-full gap-4 sm:grid-cols-3">
      {stats.map((stat) => (
        <div class="stat-card-rustic">
          <p class="font-heading text-2xl font-semibold text-brown-forest">{stat.value}</p>
          <p class="text-xs font-semibold uppercase tracking-wide text-brown-muted">{stat.label}</p>
          <p class="mt-2 text-xs text-brown-body">{stat.detail}</p>
        </div>
      ))}
    </div>
  </div>
</section>
